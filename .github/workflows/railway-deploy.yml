name: Force Railway Deployment

on:
  push:
    branches: [ minimal-prod ]
  workflow_dispatch:

jobs:
  trigger-railway:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      
    - name: Trigger Railway Deployment
      run: |
        echo "🚀 Railway deployment triggered for commit: ${{ github.sha }}"
        echo "📊 Branch: ${{ github.ref_name }}"
        echo "💾 97 modules ready for deployment"
        
    - name: Railway Deploy Webhook
      run: |
        # This will trigger Railway's GitHub integration
        echo "✅ Railway should auto-deploy from GitHub push"